{
    "name": "resume-builder",
    "version": "1.0.0",
    "description": "A simple resume builder",
    "scripts": {
        "compile": "tsc && sass styles:styles",
        "dev": "concurrently \"tsc --watch\" \"sass --watch styles:styles\" \"live-server --port=8080 --no-browser\""
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "concurrently": "^9.1.2",
        "http-server": "^14.1.1",
        "live-server": "^1.2.2"
    }
}